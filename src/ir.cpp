#include "ir.h"

void initIR() {
	// TODO: set up the RMT carrier modulator for transmit and rmt_rx for learning
}

void sendIR(const IRCode& code) {
	// TODO: emit the envelope on the IR banks at code.carrierHz
	(void)code;
}
